# 系统配置

# 系统提示词（留空则使用默认）
systemPrompt: ""

# 工具执行最大轮次
maxToolRounds: 200

# 流式输出
stream: true

# 子代理最大嵌套深度（默认 3）
# maxAgentDepth: 3

# 默认模式（需与 modes.yaml 中定义的名称对应）
# defaultMode: code

# 记录 LLM 请求日志到 logs/ 目录（调试用，默认关闭）
# 开启后每次请求会生成一个 JSON 文件，包含完整的请求 URL、headers、body
# logRequests: true
